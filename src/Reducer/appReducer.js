export const initialState = {
    favorites: []
};
export const Reducer=(state, action) =>{
    switch (action.type) {
        case 'f':
        return '1'
        break;
       
        default:
    
            return state; 
    }
}
